# datakrs

FastAdmin + LDCMS enterprise website source for datakrs.com.

## Notes

- Runtime cache, uploaded files, dependencies, database dumps, and certificates are intentionally excluded.
- Configure database credentials through environment variables or the deployment environment.
- Run `composer install` on the target server to restore PHP dependencies when needed.
